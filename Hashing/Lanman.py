def encrypt(input, privKey, pubKey):
    """Lanman Hashing algorithim

    Args:
        input (str): string data that is to be encrypted
        privKey (str): no private key. defaulted to None
        pubKey (str): no public key. defaulted to None

    Returns:
        str: returns the hash value of input using Lanman hashing algorithim
    """
    pass
